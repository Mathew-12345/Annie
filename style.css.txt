body {
    margin: 0;
    overflow: hidden; /* Para controlar el desbordamiento de animaciones */
    font-family: 'Pacifico', cursive; /* Fuente general */
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
    background: linear-gradient(to bottom right, #F4C2C2, #FFFFFF); /* Fondo rosa bebé difuminado */
    transition: background-color 1s ease-in-out;
}

#container {
    text-align: center;
    position: relative;
    padding: 20px;
}

#lotso {
    width: 300px; /* Ajusta el tamaño de Lotso */
    height: auto;
    cursor: pointer;
    transition: transform 0.1s ease-out; /* Pequeño efecto al hacer click */
    z-index: 2; /* Asegura que esté sobre el fondo */
    position: relative;
}

#lotso.clicked {
    transform: scale(1.05); /* Pequeño "salto" al clickear */
}

#message {
    color: #8B0000; /* Rojo oscuro para el texto principal */
    font-size: 1.5em;
    margin-top: 20px;
    z-index: 2;
    position: relative;
    max-width: 80%; /* Para que no se extienda demasiado en pantallas grandes */
    margin-left: auto;
    margin-right: auto;
}

#click-count {
    color: #A52A2A; /* Marrón para el contador */
    font-size: 1.2em;
    margin-top: 10px;
    z-index: 2;
    position: relative;
}

/* Estilos para el estado final de cumpleaños */
.birthday-background {
    background: linear-gradient(to bottom, #FFD1DC, #FFABAB); /* Rosas más vibrantes */
}

/* Animación de confeti y chispas */
#confetti-explosion {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none; /* Permite clicks a través de él */
    overflow: hidden;
    z-index: 100;
    opacity: 0;
    transition: opacity 0.5s ease-out;
}

#confetti-explosion.active {
    opacity: 1;
}

/* Estilos para los elementos individuales de confeti/chispas */
.confetti {
    position: absolute;
    width: 10px;
    height: 10px;
    background-color: #FFD700; /* Amarillo oro */
    border-radius: 50%;
    animation: fall 3s linear forwards;
    opacity: 0;
}

.confetti.pink { background-color: #FFC0CB; }
.confetti.blue { background-color: #ADD8E6; }
.confetti.silver { background-color: #C0C0C0; }
.confetti.sparkle {
    background-color: transparent;
    border: 2px solid #FFD700;
    width: 8px;
    height: 8px;
    transform: rotate(45deg);
    animation: fallSparkle 3s linear forwards;
}

@keyframes fall {
    0% { transform: translateY(-100px) rotate(0deg); opacity: 1; }
    100% { transform: translateY(100vh) rotate(720deg); opacity: 0; }
}

@keyframes fallSparkle {
    0% { transform: translateY(-100px) rotate(0deg); opacity: 1; }
    100% { transform: translateY(100vh) rotate(720deg); opacity: 0; }
}

/* Ocultar el mensaje y contador en la pantalla final */
#container.final-state #message,
#container.final-state #click-count {
    opacity: 0;
}

#lotso-final-message {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    color: #8B0000;
    font-family: 'Dancing Script', cursive; /* Fuente elegante para el mensaje final */
    font-size: 3em;
    text-shadow: 2px 2px 5px rgba(0,0,0,0.3);
    text-align: center;
    line-height: 1.2;
    opacity: 0;
    transition: opacity 1s ease-in-out;
    z-index: 50; /* Sobre Lotso y todo */
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    width: 90%;
}

#lotso-final-message.active {
    opacity: 1;
}

#lotso-final-message span {
    display: block;
}

#lotso-final-message .small-text {
    font-size: 0.6em; /* Mensaje de amor más pequeño */
    margin-top: 10px;
    font-family: 'Pacifico', cursive;
}

@media (max-width: 600px) {
    #lotso {
        width: 250px;
    }
    #message {
        font-size: 1.2em;
    }
    #click-count {
        font-size: 1em;
    }
    #lotso-final-message {
        font-size: 2em;
    }
    #lotso-final-message .small-text {
        font-size: 0.5em;
    }
}